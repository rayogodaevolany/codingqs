#include<iostream>
using namespace std;

// find out how many ways there are to make a number X with coins A,B, and C
int main(){
    // can you use modulo?
    // 100 % 100 is 0 then its good?
    // 100 / 100 is 1 so only one?
    // coins is recursively found no?
    // no that was just if its possible or not
    //
    //
}
